---
status: complete
phase: 03.2-keyed-tag-and-tag-filter
source: [03.2-01-SUMMARY.md, 03.2-02-SUMMARY.md]
started: 2026-04-12T00:00:00Z
updated: 2026-04-24T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Full test suite passes
expected: Run `cargo test --workspace` from the repo root. All tests pass: event-sourcing (39), event-sourcing-logstore-inmemory (20). No compilation errors, no test failures.
result: pass

### 2. TagFilter::Equals matches exactly
expected: `TagFilter::Equals("order:o1".to_string()).matches("order:o1")` returns true. `TagFilter::Equals("order:o1".to_string()).matches("order:o2")` returns false. This is verified by the `tag_filter_equals_matches_and_misses` test passing in step 1.
result: pass

### 3. TagFilter::StartsWith enforces separator rule
expected: `TagFilter::StartsWith("order:".to_string()).matches("order:abc")` returns true. `TagFilter::StartsWith("order:".to_string()).matches("order")` returns false (no trailing separator). Verified by `tag_filter_starts_with_matches_and_misses` test in step 1.
result: pass

### 4. TagFilter serde produces expected JSON format
expected: |
  Serializing TagFilter variants produces:
  - Equals("order:o1") → `{"Equals":"order:o1"}`
  - StartsWith("order:") → `{"StartsWith":"order:"}`
  - And([Equals("a"), Equals("b")]) → `{"And":[{"Equals":"a"},{"Equals":"b"}]}`
  Verified by the serde roundtrip tests passing in step 1.
result: pass

### 5. InMemoryLogStore filters events by TagFilter
expected: |
  Appending 4 events with tags `order:o1`, `order:o2`, `premium`, `shipping:ship1`, then reading with:
  - `TagFilter::StartsWith("order:")` → 2 events returned
  - `TagFilter::EndsWith(":o1")` → 1 event returned
  - `tag_filter: None` → all 4 events returned
  Verified by `inmemory_evaluates_tag_filter_variants` test passing in step 1.
result: pass

### 6. AppendCondition with TagFilter detects concurrency conflicts
expected: |
  Appending with `AppendCondition { after: pos, query: Query with StartsWith("order:") }` when a prior `order:*` event exists after `pos` returns `ConcurrencyConflict`.
  When no matching event exists after `pos`, the append succeeds.
  Verified by the two `append_condition_with_starts_with_filter_*` tests in step 1.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
